spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    com.zomato.recommendation: DEBUG
    org.springframework.web: INFO

app:
  data:
    cache-path: classpath:data/processed/restaurants.json
    raw-path: data/raw
    auto-ingest-on-startup: false
